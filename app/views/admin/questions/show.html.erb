
<h1><%= @question.title %></h1>

<h2>
  Answers
</h2>

<%=link_to 'New answer', new_admin_question_answer_path(@question) %>

<ol>
  <%= render partial: 'admin/answers/answer', collection: @question.answers %>
</ol>

<%= link_to "<< Back to #{ @question.test.title }", admin_test_path(id: @question.test_id) %>
<%= link_to 'Delete',
                admin_question_path(@question),
                method: :delete,
                data: { confirm: "Are you shure?" }%>
