<tr>
  <td><%= badge.title %></td>
  <td class:="align-middle">
    <%= link_to t('.edit'), edit_admin_badge_path(badge)%>
  </td>
  <td class:="align-middle">
    <%= link_to t('.delete'),
                admin_badge_path(badge),
                method: :delete,
                data: { confirm: t('.sure?') } %>
</tr>
